// Create a class component
   // Import React and the Component method from react

   // Create a class that extends the Component class

   // Include a render method in the class which returns some jsx

   // Export the class as the default

// Add state
   // Add a constructor to this class and call the super method
   // Set a property called state in the constructor to an object.
   // Add a property to the state and give it an initial value
   // Render the state property in the return statement of the render method

// Add user interaction
   // Now add an input element that allows a user to change the name

   // Set the value attribute of the input to the name on the state
   
   // Add a class method to handle the name change

   // Use the onChange attribute of the input to register the name change handler

// Finally, let's make it better
   // Add another property for inputName and a matching handler to upate the inputName

   // Switch the value and handler on the input element to match the new property and method
      // This will leave the display name unchanged while the user is typing
   
   // Add a button to set the name on state once the user clicks the button